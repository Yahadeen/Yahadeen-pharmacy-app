# Metro (React Native's bundler) cannot follow pnpm's symlinked store, so we ask
# pnpm to lay out a flat node_modules like npm would. Required for Expo in a
# workspace — do not remove.
node-linker=hoisted
shamefully-hoist=true
strict-peer-dependencies=false
auto-install-peers=true
